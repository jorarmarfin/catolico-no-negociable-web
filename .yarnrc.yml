nodeLinker: node-modules

# Yarn 4 bloquea por defecto paquetes publicados hace poco tiempo ("quarantine")
# como protección ante supply-chain attacks. Varias dependencias de este proyecto
# (@astrojs/*, tailwindcss) reciben releases frecuentes y caían en esa ventana,
# bloqueando `yarn install` aunque ya estaban instaladas y probadas vía npm.
npmMinimalAgeGate: 0
